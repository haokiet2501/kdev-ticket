import { useSelector, useDispatch }  from 'react-redux'
import { getTicket, reset }

const Ticket = () => {
    return (
        <div>
            Ticket
        </div>
    )
}

export default Ticket
