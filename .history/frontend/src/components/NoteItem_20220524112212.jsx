import { useSelector } from 'react-redux'

const NoteItem = ({ note }) => {
    const {user} = useSelector((stae) )
    return (
        <div>
        
        </div>
    )
}

export default NoteItem
